columns:
    date:
        label: Fecha
        type: text
        sortable: true
        select: concat(date_format(date,"%d %b, %Y"),"&nbsp;&nbsp;a&nbsp;&nbsp;",date_format(date_add(date,interval 1 day),"%d %b, %Y"))
